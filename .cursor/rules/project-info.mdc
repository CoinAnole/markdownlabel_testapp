---
alwaysApply: true
---
- Stack: Kivy app demoing `kivy_garden.markdownlabel`; Python 3.
- Entry point: `main.py` builds a ScrollView → vertical BoxLayout of sections (`font_name`, `font_size`, `color`, `line_height`, `halign`, `padding`, `disabled`); window set to 800x1200 with title "MarkdownLabel Demo - Label Compatibility".
- Content: all `MarkdownLabel` widgets use the `SAMPLE_MARKDOWN` constant in `main.py`; `sample_markdown.md` is separate demo content used by tests and includes headings H1-H6, inline formatting, lists, tables, code block, blockquote, and links.
- Event handling: `on_ref_press` prints `Link clicked: {ref}`; background rectangles for padding demo updated via `_update_rect`.
- Dependencies: `requirements.txt` (kivy>=2.0.0, mistune>=3.0.0, pytest>=7, hypothesis>=6); also need `kivy_garden.markdownlabel` installed (README recommends `python3 -m pip install -e external/markdownlabel`).
- Tests: `python3 -m pytest tests/`; assertions cover window/title config, scroll layout, section/variation counts, Markdown content completeness, README setup/troubleshooting sections, handler robustness with arbitrary refs.
- External: `external/markdownlabel/` expected to hold the garden flower; directory may be empty if submodule not pulled.
